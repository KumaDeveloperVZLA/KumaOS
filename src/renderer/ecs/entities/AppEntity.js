// Plantilla para la Entidad Aplicación
export class AppEntity {
  constructor(manifest) {
    this.name = "AppEntity";
    this.components = [
      // new AppManifest(manifest),
      // new ProcessState()
    ];
  }
}
